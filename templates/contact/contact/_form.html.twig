{{ form_start(form) }}

    {{ form_row(form.firstName, {"label": "Prénom"}) }}
    {{ form_row(form.lastName, {"label": "Nom de famille"}) }}
    {{ form_row(form.email, {"label": "Adresse e-mail"}) }}

    {{ form_row(form.priority, {"attr": {"min" : 1, "max": 3}, "label": "Priorité"}) }}
    <small id="contact_priority" class="form-text text-muted">Faible priorité: 3. Haute priorité: 1.</small>
    <br>
    {{ form_widget(form) }}
    <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
